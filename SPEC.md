# 网络设备配置备份应用 - 规格文档

## 1. 项目概述

**项目名称**: NetworkDeviceBackup
**项目类型**: Java Swing 桌面应用
**核心功能**: 支持多品牌型号的网络设备配置备份管理，包括设备资产列表、品牌命令模板、任务执行与结果记录
**目标用户**: 网络运维工程师、系统管理员

---

## 2. UI/UX 规格

### 2.1 布局结构

**主窗口**: 1200x800 像素，支持最大化
**布局方式**: 左侧导航 + 右侧内容区

**主要区域**:
- **顶部**: 工具栏（含主要操作按钮）
- **左侧**: 导航菜单（200px宽）
- **右侧**: 主内容区（设备列表/命令配置/任务日志）

### 2.2 组件设计

**颜色方案**:
- 主色: #2C3E50 (深蓝灰)
- 次色: #3498DB (亮蓝)
- 强调色: #27AE60 (成功绿), #E74C3C (错误红), #F39C12 (警告橙)
- 背景: #ECF0F1 (浅灰)
- 文字: #2C3E50 (主), #7F8C8D (次)

**字体**: Microsoft YaHei / SansSerif, 平滑模式

**组件样式**:
- 按钮: 圆角3px, 内边距8px 16px
- 表格: 斑马纹行，选中行高亮 #3498DB
- 输入框: 边框 #BDC3C7, 聚焦边框 #3498DB
- 卡片: 白色背景，1px边框，圆角5px，阴影

### 2.3 页面结构

1. **设备资产列表页**
   - 工具栏：添加/编辑/删除/导入/导出/备份按钮
   - 搜索过滤栏
   - 设备表格（复选框、ID、品牌、型号、IP地址、端口、用户名、状态、最后备份时间）

2. **品牌命令配置页**
   - 左侧：品牌树形列表（华为、H3C、思科、锐捷等）
   - 右侧：选中品牌的命令模板编辑器

3. **任务日志页**
   - 上部：任务列表（任务ID、设备、状态、开始时间、结束时间）
   - 下部：日志详情（命令回显、错误信息）

---

## 3. 功能规格

### 3.1 设备资产管理

**设备信息字段**:
- ID (自增)
- 品牌 (华为/H3C/思科/锐捷/中兴/迪普)
- 型号 (字符串)
- IP地址 (IPv4)
- 端口 (默认22)
- 用户名 (字符串)
- 密码 (加密存储)
- 设备状态 (在线/离线/未知)
- 备注 (文本)
- 创建时间
- 最后备份时间

**操作功能**:
- 添加设备（表单对话框）
- 编辑设备
- 删除设备（确认对话框）
- 批量选择
- 批量备份
- 按品牌/状态过滤
- 搜索（IP/型号）
- 导入导出（JSON格式）

### 3.2 品牌命令模板

**支持的品牌**:
- 华为 (Huawei)
- H3C
- 思科 (Cisco)
- 锐捷 (Ruijie)
- 中兴 (ZTE)
- 迪普 (DPtech)

**每品牌的命令模板**:
- 连接测试命令
- 配置备份命令（如 `display current-configuration`）
- 保存命令（如 `save`）

### 3.3 任务执行与日志

**任务状态**:
- 等待中 (WAITING)
- 执行中 (RUNNING)
- 成功 (SUCCESS)
- 失败 (FAILED)
- 部分成功 (PARTIAL_SUCCESS)

**任务信息**:
- 任务ID
- 设备ID
- 设备IP
- 设备品牌
- 任务状态
- 开始时间
- 结束时间
- 命令回显（stdout）
- 错误信息（stderr）

**日志功能**:
- 实时显示任务进度
- 显示命令回显结果
- 显示错误详情
- 日志级别（INFO/ERROR）
- 按任务/设备/时间过滤
- 导出日志

### 3.4 数据存储

**SQLite数据库**:
- `devices` 表：设备资产
- `brands` 表：品牌配置
- `commands` 表：命令模板
- `tasks` 表：任务记录
- `logs` 表：日志记录

---

## 4. 技术架构

**技术栈**:
- Java 8+
- Swing (GUI)
- SQLite (数据库)
- JSCH (SSH连接)
- Maven (构建)

**目录结构**:
```
src/
├── main/java/com/nbbackup/
│   ├── Main.java
│   ├── ui/
│   │   ├── MainFrame.java
│   │   ├── DevicePanel.java
│   │   ├── BrandCommandPanel.java
│   │   ├── TaskLogPanel.java
│   │   └── dialogs/
│   ├── model/
│   │   ├── Device.java
│   │   ├── Brand.java
│   │   ├── CommandTemplate.java
│   │   ├── BackupTask.java
│   │   └── TaskLog.java
│   ├── service/
│   │   ├── DeviceService.java
│   │   ├── BrandService.java
│   │   ├── TaskService.java
│   │   └── SshService.java
│   ├── dao/
│   │   └── DatabaseHelper.java
│   └── util/
│       └── PasswordUtil.java
```

---

## 5. 验收标准

1. ✅ 可以添加、编辑、删除网络设备
2. ✅ 设备列表支持按品牌、状态过滤和搜索
3. ✅ 支持多品牌（Huawei/H3C/Cisco/Ruijie/ZTE/DPtech）的命令模板
4. ✅ 可以执行单个或批量设备备份任务
5. ✅ 任务进度实时显示
6. ✅ 命令回显结果和错误信息完整记录
7. ✅ 日志支持按任务、设备过滤
8. ✅ 数据持久化存储到SQLite
9. ✅ 密码加密存储
10. ✅ UI界面清晰、操作流畅
